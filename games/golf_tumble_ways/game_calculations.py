"""Game specific calculations for golf_tumble_ways. Mirrors the minimal
pass-through shape confirmed in games/0_0_ways/game_calculations.py —
we don't need board-multiplier-symbol logic (that's the "M" symbol
mechanic from 0_0_scatter, which we deliberately aren't using; see
README/game_config.py notes)."""
from src.executables.executables import Executables


class GameCalculations(Executables):
    pass
